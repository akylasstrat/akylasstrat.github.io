# Use the local MiKTeX executables directly. This avoids relying on VS Code's PATH.
$pdflatex = '"C:/Users/ucbva19/AppData/Local/Programs/MiKTeX/miktex/bin/x64/pdflatex.exe" %O %S';

# Force biber (not bibtex)
$biber = '"C:/Users/ucbva19/AppData/Local/Programs/MiKTeX/miktex/bin/x64/biber.exe" %O %S';

# Good defaults
$pdf_mode = 1;         # build PDF via (pdf)LaTeX
$diagnostics = 1;      # better messages

# If you ever see "Nothing to do" but you changed .bib, force a rebuild:
#    latexmk -g -pdf akylas-cv.tex
